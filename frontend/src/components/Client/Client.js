import React from 'react';
import ReactVirtualizedTable from './clientListTable';

const Client = () => {
    return (
        <div>
            <ReactVirtualizedTable/>
        </div>
    );
};

export default Client;